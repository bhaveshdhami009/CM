// This file is used when you run 'ng build'
export const environment = {
  production: true,
  // In production, the API and Frontend are on the same domain, so the URL is relative.
  apiUrl: '/api' 
};