export interface CreateLogDto {
  // Dates usually arrive as strings from the API/FormData
  log_date: string; 
  purpose: string;
  
  // Optional fields
  remarks?: string;
  
  is_pending?: boolean;
  
  // The file path generated by Multer
  file_path?: string | null;
  file_extension?: string | null;
}