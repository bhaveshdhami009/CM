{
  "scripts": {
    "build": "cd frontend && npm install && npx ng build --configuration production && cd ../backend && npm install && npm run build && npm run seed",
    "start": "cd backend && npm start"
  }
}
